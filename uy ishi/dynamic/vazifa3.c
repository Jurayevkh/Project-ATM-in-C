#include <stdio.h>

int main(){
    char soz[20];
    char *ptr=calloc(20, sizeof(char));
    printf("So'z kiriting:");
    scanf("%s", ptr);
    for (int i = 0; i <20; i++)
    {
        
    }
    
    
}